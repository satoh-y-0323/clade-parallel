# テストレポート（T18 最終）

## 実行日時
2026-04-20 (タスク T18: 全テスト再実行 + カバレッジ再計測)

## 担当タスク
**T18** — 全テスト再実行 + カバレッジ再計測（最終 test-report 作成）

## 参照したレポート
- 要件定義: requirements-report-20260420-221945.md
- アーキテクチャ: architecture-report-20260420-223210.md
- 計画（更新版）: plan-report-20260421-003014.md
- 前回 test-report（T13 Red）: test-report-20260421-004152.md
- code-review-report（T10）: code-review-report-20260420-234958.md
- security-review-report（T11）: security-review-report-20260421-002428.md

## テスト対象
- `src/clade_parallel/__init__.py`
- `src/clade_parallel/_exceptions.py`（R4: 新設）
- `src/clade_parallel/cli.py`
- `src/clade_parallel/manifest.py`
- `src/clade_parallel/runner.py`

---

## テスト結果サマリ

### fast path（`python -m pytest -q`）
```
91 passed, 2 deselected in 1.07s
```
- 合格: **91 件**
- 不合格: 0 件
- deselected（slow 除外）: 2 件

### slow path（`python -m pytest -q -m slow`）
```
2 passed, 91 deselected in 0.11s
```
- 合格: **2 件**
- 不合格: 0 件

### 合計
- **fast 91 件 + slow 2 件 = 93 件 全件合格**

---

## カバレッジ（`python -m pytest --cov=clade_parallel --cov-report=term tests/`）

fast path のみ（デフォルト: slow 除外）での計測結果:

```
Name                                Stmts   Miss  Cover
-------------------------------------------------------
src\clade_parallel\__init__.py          5      0   100%
src\clade_parallel\_exceptions.py       1      0   100%
src\clade_parallel\cli.py              57      1    98%
src\clade_parallel\manifest.py         86      4    95%
src\clade_parallel\runner.py           79      1    99%
-------------------------------------------------------
TOTAL                                 228      6    97%
```

- **総合カバレッジ: 97%**（目標 80% を大幅に上回る）
- 前回 test-report（T9）の 96% から 97% に改善（R4 で `_exceptions.py` 100% カバー追加）

---

## 静的解析

### ruff（`ruff check src/ tests/`）
```
All checks passed!
```
**判定: PASS**

### black（`black --check src/ tests/`）
```
Warning: Python 3.10 cannot parse code formatted for Python 3.14. (safety check note)
All done! ✨ 🍰 ✨
11 files would be left unchanged.
```
**判定: PASS**（警告は Python バージョン差異によるパーサー互換性の注意のみ。フォーマット整合性は確認済み）

### mypy（`mypy src/ tests/`）
```
Success: no issues found in 11 source files
```
**判定: PASS**

---

## R4 例外階層確認（`python -c "..."` 実行）

```
python -c "from clade_parallel import CladeParallelError, ManifestError, RunnerError; \
  assert issubclass(ManifestError, CladeParallelError) and issubclass(RunnerError, CladeParallelError); \
  print('OK')"
→ OK
```
**判定: PASS** — `CladeParallelError` が公開 API として機能し、継承階層が正しく構成されている

---

## 疎結合確認（`import clade` / `from clade ` 検索）

`src/` および `tests/` 配下の全 `.py` ファイルを検索した結果:
```
OK: No forbidden clade imports found
```
- `from clade_parallel` は自パッケージのため除外（正常）
- それ以外の `import clade` / `from clade ` は **0 件**

**判定: PASS**

---

## 要件との照合表

| 要件（完了条件）| 対応テスト名 | 結果 |
|---|---|---|
| read-only 並列実行（複数エージェントを同時に起動） | `test_parallel_execution_is_faster_than_serial`, `test_two_tasks_run_in_different_threads` | 合格 |
| race condition 防止（共有状態の安全な更新） | `test_shared_counter_reaches_2_without_race_condition` | 合格 |
| タイムアウト後の子プロセス kill | `test_タイムアウト時に子プロセスがkillされる` | 合格 |
| タイムアウト発生時に他タスクは継続 | `test_タイムアウトが発生してもtimed_outがTrueになり他タスクは継続する` | 合格 |
| task.env の危険キー（LD_PRELOAD 等）ブロック | `test_危険な環境変数キーはManifestErrorを送出する[LD_PRELOAD]` 他 6 件 | 合格 |
| 通常の env キー（FOO 等）は許可 | `test_通常の環境変数キーは許可される` | 合格 |
| 複数 RunnerError は最初の 1 件のみ送出 | `test_複数タスクがRunnerErrorを発生させても最初の1件のみ送出する` | 合格 |
| CLI exit code: 全成功=0 | `test_all_tasks_success_returns_exit_0` | 合格 |
| CLI exit code: 部分失敗=1 | `test_partial_failure_returns_exit_1` | 合格 |
| CLI exit code: ManifestError=2 | `test_manifest_error_returns_exit_2_and_writes_stderr` | 合格 |
| CLI exit code: RunnerError=3 | `test_runner_error_returns_exit_3_and_writes_stderr` | 合格 |
| --version で 0.1.0 表示 | `test_version_flag_prints_version_and_returns_exit_0` | 合格 |
| マニフェスト YAML パース正常系 | `test_正常なマニフェストがパースできる` | 合格 |
| clade_plan_version 未サポートで ManifestError | `test_サポートバージョン外はManifestErrorを送出する` (6 ケース) | 合格 |
| read_only: false 指定で ManifestError | `test_read_only_falseのタスクはManifestErrorを送出する` | 合格 |
| CladeParallelError 公開 API | R4 確認コマンド（import + isinstance チェック） | 合格 |
| 疎結合（`import clade` ゼロ） | 静的 grep 検索 | 合格 |
| クロスプラットフォーム（slow 実プロセス） | `TestSlowSmokeRealProcess::test_real_process_both_tasks_exit_0` | 合格 |
| slow スモークテスト（プラットフォーム別ラッパー） | `test_実プロセス経由のスモークテスト` | 合格 |

---

## T10/T11 指摘の解消状況

| 指摘 | 内容 | 対応タスク | 解消確認 |
|---|---|---|---|
| T10 ブロッカー #1（subprocess zombie） | TimeoutExpired 後の子プロセス残存 | F1（T14） | `test_タイムアウト時に子プロセスがkillされる` 合格 |
| T10 ブロッカー #2（多重 RunnerError） | runner.py L233 の `else: raise exc` 誤処理 | F2（T15） | `test_複数タスクがRunnerErrorを発生させても最初の1件のみ送出する` 合格 |
| T10 推奨 R1（slow テスト修正） | test_スモークテスト をラッパースクリプト方式に | R1（T17） | `test_実プロセス経由のスモークテスト` 合格（slow 2 件全 pass） |
| T10 推奨 R2（_parse_task 未使用引数） | `default_agent_prompt` 削除 | R2（T17） | mypy / ruff 全クリアで確認 |
| T10 推奨 R3（sys.exit → return 統一） | cli.py L162-163 の sys.exit を return に | R3（T17） | `test_no_args_returns_nonzero` 合格、cli.py カバレッジ 98% |
| T10 推奨 R4（CladeParallelError ベース） | `_exceptions.py` 新設・継承切替 | R4（T17） | `from clade_parallel import CladeParallelError` → OK |
| T11 High #A（= T10 #1 と同一） | Popen + kill で解消 | F1（T14） | 上記 T10 #1 と同一 |
| T11 High #B（env injection） | `LD_PRELOAD` 等ブロックリスト追加 | F3（T16） | parametrize 6 ケース全 pass |

**ブロッカー 2 件・High 2 件: 全解消確認済み**

---

## R1〜R4 完了状況

| 項目 | 内容 | 状態 |
|---|---|---|
| R1 | slow テスト（test_スモークテスト）をラッパースクリプト方式に修正 | 完了（slow 2 件全 pass） |
| R2 | `_parse_task(…, default_agent_prompt)` 未使用引数削除 | 完了（mypy クリア） |
| R3 | `cli.py:L162-163` の `sys.exit` を `return` に統一 | 完了（cli.py 98% カバレッジ） |
| R4 | `CladeParallelError` ベースクラス導入・公開 API 化 | 完了（`_exceptions.py` 100% カバレッジ） |

**R1〜R4 全 4 件: 完了**

---

## クロスプラットフォーム留意点

- **Windows 実機（本 PC）**: Windows 11 Home 10.0.26200 + Python 3.10.6 で fast 91 件 / slow 2 件 全件 pass 確認済み
- **macOS / Linux**: 設計上対応済み
  - `pathlib.Path` 統一（`os.path` 文字列操作なし）
  - slow テスト（R1 対応済み）: `sys.platform == "win32"` 分岐により `.bat` / `.sh` を使い分けるラッパースクリプト方式
  - `subprocess.Popen` は Unix / Windows 両対応

---

## DoD チェックリスト評価（T21 用プレビュー）

| # | 条件 | 判定 | 根拠 |
|---|---|---|---|
| 1 | fast path テスト全件合格（82 件 → 91 件） | **合格** | `91 passed, 2 deselected in 1.07s` |
| 2 | slow path テスト全件合格 | **合格** | `2 passed, 91 deselected in 0.11s`（R1 完了済み） |
| 3 | カバレッジ 80% 以上 | **合格** | 総合 97%（fast path 計測） |
| 4 | 静的解析（ruff / black / mypy）全クリア | **合格** | 3 ツール全て PASS |
| 5 | T19 code-reviewer ブロッカー指摘ゼロ | T19 担当 | — |
| 6 | T20 security-reviewer High 指摘ゼロ | T20 担当 | — |
| 7 | 疎結合（`import clade` / `from clade ` ゼロ） | **合格** | grep 検索 0 件 |
| 8 | `CladeParallelError` 公開 API に含まれている | **合格** | `from clade_parallel import CladeParallelError` + isinstance チェック OK |

テスト観点から判定可能な 1〜4 / 7〜8 の **6 項目は全て達成**。5・6 は T19/T20 のレビュー担当。

---

## 合格したテスト（全 93 件）

### fast path（91 件）

**test_cli.py（15 件）:**
- [x] TestMainExitCodes::test_all_tasks_success_returns_exit_0
- [x] TestMainExitCodes::test_partial_failure_returns_exit_1
- [x] TestMainExitCodes::test_manifest_error_returns_exit_2_and_writes_stderr
- [x] TestMainExitCodes::test_runner_error_returns_exit_3_and_writes_stderr
- [x] TestVersionFlag::test_version_flag_prints_version_and_returns_exit_0
- [x] TestHelpFlag::test_help_flag_returns_exit_0
- [x] TestNoArgumentsError::test_no_args_returns_nonzero
- [x] TestQuietFlag::test_quiet_suppresses_progress_output
- [x] TestQuietFlag::test_quiet_still_shows_failure_summary
- [x] TestMaxWorkersPassthrough::test_max_workers_forwarded_to_run_manifest
- [x] TestClaudeExePassthrough::test_claude_exe_forwarded_to_run_manifest
- [x] TestSummaryOutput::test_summary_line_contains_expected_label[0-False-ok]
- [x] TestSummaryOutput::test_summary_line_contains_expected_label[1-False-fail]
- [x] TestSummaryOutput::test_summary_line_contains_expected_label[None-True-timeout]
- [x] TestSummaryOutput::test_summary_line_contains_agent_and_duration

**test_integration.py（9 件 fast + 2 件 slow のうち fast 9 件）:**
- [x] TestBothTasksSucceed::test_overall_ok_is_true_when_both_tasks_exit_0
- [x] TestBothTasksSucceed::test_task_ids_match_manifest_declarations
- [x] TestBothTasksSucceed::test_task_agents_match_manifest_declarations
- [x] TestParallelExecution::test_two_tasks_run_in_different_threads
- [x] TestParallelExecution::test_parallel_execution_is_faster_than_serial
- [x] TestRaceConditionGuard::test_shared_counter_reaches_2_without_race_condition
- [x] TestCliEndToEnd::test_cli_returns_exit_0_and_prints_ok_summary
- [x] TestCliEndToEnd::test_cli_summary_contains_ok_lines_for_each_task
- [x] TestCliEndToEnd::test_cli_returns_exit_1_when_one_task_fails

**test_manifest.py（42 件）:** 全 42 件 PASS（マニフェストパース・バージョン検証・必須キー検証・frozen dataclass・env ブロックリスト 6 件 + 通常キー許可 1 件を含む）

**test_runner.py（25 件 fast のうち slow 除く 24 件）:**
- [x] test_全タスクが成功した場合は全体成功となる
- [x] test_一部タスクが失敗した場合は全体失敗となる
- [x] test_タイムアウトが発生してもtimed_outがTrueになり他タスクは継続する
- [x] test_任意例外が発生時stderr にトレースバックが保存され他タスクは継続する
- [x] test_claudeバイナリが見つからない場合はRunnerErrorを送出する
- [x] test_subprocess_runに成功時に結果が正しく渡される
- [x] test_task_envがos_environにマージされPATHが維持される
- [x] test_タスク間で互いに実行時間が重なり合計より短い
- [x] test_max_workers_1を指定すると直列実行になる
- [x] test_run_manifestがPathを受け付ける
- [x] test_run_manifestがstrを受け付ける
- [x] test_run_manifestがManifestオブジェクトを受け付ける
- [x] test_TaskResult_okプロパティの確認[exit0/1/2/127/exit_neg1]（5 件）
- [x] test_RunResult_overall_okは全タスク成功のみTrue
- [x] test_RunResult_overall_okは一部失敗ならFalse
- [x] test_TaskResultのフィールドが正しく設定される
- [x] test_RunResult_resultsはタプルである
- [x] test_TaskResultはfrozenデータクラスである
- [x] test_タスクの実行に異なるスレッドが使われる
- [x] **test_タイムアウト時に子プロセスがkillされる** ← F1 修正確認
- [x] **test_複数タスクがRunnerErrorを発生させても最初の1件のみ送出する** ← F2 修正確認

### slow path（2 件）
- [x] TestSlowSmokeRealProcess::test_real_process_both_tasks_exit_0
- [x] **test_実プロセス経由のスモークテスト** ← R1 修正確認

---

## 不合格・要修正
**なし** — 93 件全て合格

---

## 要件未充足の項目
**なし** — テスト観点での要件充足は全て確認済み

---

## 総合判定

**v0.1 MVP の DoD 達成条件をテスト観点から満たしている**

- fast path 91 件全件 pass
- slow path 2 件全件 pass（R1 修正によりラッパースクリプト方式に変更）
- 総合カバレッジ 97%（目標 80% を大幅超過、前回 96% から改善）
- 静的解析（ruff / black --check / mypy）全クリア
- T10 ブロッカー #1/#2・T11 High #A/#B の全解消を実テストで確認
- R1〜R4 の中優先修正全 4 件完了
- 疎結合維持（`import clade` / `from clade ` ゼロ）
- `CladeParallelError` 公開 API 充足（ManifestError / RunnerError の継承階層も正常）

T19（code-reviewer）・T20（security-reviewer）でのレビュー確認を経て T21 DoD 最終判定を行う段取りに問題なし。

---

## developerへの依頼事項
**なし** — テスト観点での修正依頼はゼロ件
